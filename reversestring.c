#include<stdio.h>
#include<string.h>
int main()
{
    char a[20];
    puts("SAVIOUR VICTORY\n");
    puts("enter the string= ");
    gets(a);
    puts("REVERSE THE STRING=");
    puts(strrev(a));
    return 0;
}