#include<stdio.h>
#include<string.h>
int main()
{
    char a[20];
    int length;
    printf("SAVIOUR VICTORY\n");
    printf("entre the string");
    gets(a);
    length=strlen(a);
    printf("length of string=%d",length);
    return 0;
    }